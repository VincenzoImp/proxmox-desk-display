services:
  bridge:
    build:
      context: ../apps/bridge
    image: proxmox-desk-display-bridge:local
    container_name: proxmox-desk-display
    restart: unless-stopped
    ports:
      - "8765:8765"
    volumes:
      - proxmox-desk-display-data:/data

volumes:
  proxmox-desk-display-data:
