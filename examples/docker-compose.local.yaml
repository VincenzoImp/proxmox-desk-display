services:
  bridge:
    build:
      context: ../apps/bridge
    image: proxmox-desk-display-bridge:local
    container_name: proxmox-desk-display
    restart: unless-stopped
    ports:
      - "8765:8765"
    volumes:
      - pve-desk-data:/data

volumes:
  pve-desk-data:
