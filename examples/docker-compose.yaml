services:
  bridge:
    image: vincenzoimp/proxmox-desk-display-bridge:latest
    container_name: proxmox-desk-display
    restart: unless-stopped
    ports:
      - "8765:8765"
    volumes:
      - proxmox-desk-display-data:/data

volumes:
  proxmox-desk-display-data:
